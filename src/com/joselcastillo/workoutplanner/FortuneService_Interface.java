package com.joselcastillo.workoutplanner;

/* Defining the dependency interface and its class called 
 * HappyFortuneService in a separate file. */
public interface FortuneService_Interface {

	public String getFortune();
	
}
